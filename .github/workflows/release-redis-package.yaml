name: Publish Redis Node.js Package

on:
  workflow_dispatch: # Manually run the workflow

jobs:
  publish-redis:
    name: Publish Redis Package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: publish.yaml
        with:
          github-working_directory: redis
